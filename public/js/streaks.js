function dayKey(value){const d=new Date(value);return [d.getFullYear(),String(d.getMonth()+1).padStart(2,"0"),String(d.getDate()).padStart(2,"0")].join("-")}
export function calculateStats(data){const workouts=data.workouts||[];const days=[...new Set(workouts.map(w=>dayKey(w.completedAt)))].sort().reverse();let streak=0;if(days.length){const today=new Date();today.setHours(0,0,0,0);const latest=new Date(days[0]+"T00:00:00");const gap=Math.round((today-latest)/86400000);if(gap<=1){let expected=new Date(latest);for(const day of days){if(dayKey(expected)!==day)break;streak++;expected.setDate(expected.getDate()-1)}}}const totalMinutes=Math.round(workouts.reduce((n,w)=>n+w.activeSeconds,0)/60);const totalRounds=workouts.reduce((n,w)=>n+w.rounds,0);const longest=Math.max(data.longestStreak||0,streak);return{currentStreak:streak,longestStreak:longest,workouts:workouts.length,totalMinutes,totalRounds,lastWorkout:workouts[0]?.completedAt||null}}
